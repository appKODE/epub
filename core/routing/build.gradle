plugins {
  id 'android-library-convention'
  id("compose-convention")
}

android {
  namespace = 'ru.kode.epub.core.domain'
}

dependencies {
  api project(':core:domain')
  api project(':core:ui')
  api project(':core:uikit')

  api libs.bundles.decompose
}
